FROM qwen3.5

# 32K context window — comfortable fit in 16GB VRAM (4080 Super) with Q4 weights (~5-6GB) + KV cache (~4GB).
# Think mode is disabled at the API level via options.think=false (not a Modelfile parameter).
PARAMETER num_ctx 32768
