# Host-side Councilor venv (repo-root .venv) — councilor.py runs on bare host,
# NOT from the icarus-worker image, so backend/requirements.txt does not apply
# to it. Install with:  .venv/bin/python -m pip install -r requirements-councilor.txt
#
# Core: Redis IPC, local llama-server client, the subagent task registry.
redis>=5.0.0
httpx>=0.27.0
sqlalchemy>=2.0.25
aiosqlite>=0.19.0
# Delegated tasks run in this process with exactly their declared capabilities;
# gmail_read / calendar_read need the Google client libraries here too, or the
# task fails fast with "dependency missing in this process".
google-api-python-client>=2.115.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
